apply plugin: 'java'

task wrapper(type: Wrapper) {
    gradleVersion = '1.9'
}

jar {
    version '0.5'

    from(projectDir) {
        include 'installed.txt'
        include 'IntelliJ IDEA Global Settings'
        include 'colors/**'
        include 'options/**'
    }
}